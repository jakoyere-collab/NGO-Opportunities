# Open questions for this application

Filled in by Claude while comparing the job description against your CV.
Each item below is a job requirement your CV doesn't clearly show evidence
for. Nothing gets written into your CV or cover letter as fact until it's
either backed by your CV or answered here (or answered directly in chat) —
if you don't have relevant experience for something, say so and it gets
left out rather than invented.

Answer inline by replacing the `[ ]` with `[x]` and adding your answer
underneath, or just answer directly in the chat with Claude.

Format for each question (quoted here only as an example — Show-Applications.ps1
counts unquoted `- [ ]` lines as open, so leave this blockquote as-is and add
real questions as plain `- [ ]` lines below it):

> - [ ] The job description asks for "experience with DHIS2 or a similar
>       health information system." Your CV doesn't mention this. Do you
>       have relevant experience? If yes, which system(s), what did you
>       actually do with it, and roughly when/where? If no, just say so.

*(No open questions yet — Claude adds them here after comparing this
application's job description against your CV.)*
